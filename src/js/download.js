export const downloadOnScreen = (e) => (download, AllCards) => {
    if (e.target.closest('.bottom-bar__link-download')){
        let onScreen = document.createElement('img');
        // onScreen.src = 
            console.log(e.target);
        
    }
    
}